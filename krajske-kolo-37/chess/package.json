{
  "name": "bun-boilerplate",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && bun build ./src/index.ts --minify --splitting --outdir dist/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "image-hash": "^5.3.2",
    "jimp": "^0.22.12",
    "sharp": "^0.33.3"
  }
}